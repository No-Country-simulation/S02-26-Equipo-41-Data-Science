services:
  frontend:
    build: ./front-end
    container_name: frontend-container
    ports:
      - "8081:80"
    restart: always

  backend:
    build: ./backend
    container_name: backend-container
    ports:
      - "3000:3000"
    restart: always